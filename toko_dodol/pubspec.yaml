name: toko_dodol
description: Aplikasi Toko Dodol dengan Flutter dan Supabase
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^1.10.25

  # firebase_auth TIDAK dipakai di project kamu sekarang
  # jadi lebih aman dihapus agar tidak konflik
  # firebase_auth: ^4.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
